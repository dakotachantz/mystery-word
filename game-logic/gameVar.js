let game = {
    guessCounter: guessCounter = 8,
    mysteryWord: mysteryWord = '',
    guessArray: guessArray = [],
    displayArray: displayArray = [],
    gameOver: false,
}

module.exports = game;